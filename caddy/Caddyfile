{
  # Правильная подстановка переменных окружения в Caddy:
  # используем {$VAR}, а не {{env "VAR"}}
  email {$EMAIL}
}

# Обслуживаем корневой домен и www на одном бэкенде
{$DOMAIN}, www.{$DOMAIN} {
  encode zstd gzip
  reverse_proxy web:3000
}
